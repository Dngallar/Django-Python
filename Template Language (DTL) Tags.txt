
Substitution  {{ zap }}
              {{ zap|safe }}

Calling code  {% url 'car-detail' cat.id %}
              {% author.get_absolute_url %}

Logic         {% if zap > 100 %}
              {% endif %}

Blocks        {% block content %}
              {% endblock %}

Loops         {% for x in exes %}
              {% endfor %}

Nested        {{ outer.inner }} -> Show 42

              def nested(request):
                  x = {'outer' : { 'inner' : '42' }}
                  return render(request, 'tmpl/nested.html', x)

Notes:
- The command |safe is used to disable the escape in a entry.
- The command |length is used to get the length of the respective entry.
